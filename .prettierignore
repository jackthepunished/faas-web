dist
node_modules
package-lock.json

# Generated by @tanstack/router-plugin on every dev run and build — formatting
# it only guarantees a diff the next time Vite starts.
src/routeTree.gen.ts

# Generated from api/openapi.yaml by `npm run api:types`. The generator owns
# its own formatting; reformatting it only produces a diff on the next run.
src/lib/api/schema.d.ts

# Vendored verbatim from poyrazK/faas so the diff against upstream stays readable.
api/openapi.yaml

# Vendored component manifests, kept byte-identical to their source.
dither-kit.json
components.json
