# Gregale

Marketing site and product console for a fictional serverless platform — a
single-page React app with no backend. Everything behind the login is driven by
in-memory fixtures, so the whole product can be explored end to end without a
server running anywhere.

## Getting started

```bash
npm install
npm run dev          # http://localhost:3000
```

There is nothing to configure — no `.env`, no API keys, no database. To reach
the console, sign in with any email address and the demo code **`123456`**
(`DEMO_CODE` in `src/lib/auth.tsx`, and shown as a hint in the UI).

## Scripts

| Command             | What it does                                      |
| ------------------- | ------------------------------------------------- |
| `npm run dev`       | Vite dev server on port 3000                      |
| `npm run build`     | Typecheck, then production build to `dist/`       |
| `npm run preview`   | Serve the built `dist/` locally                   |
| `npm run typecheck` | `tsc --noEmit` — types only, no build             |
| `npm run lint`      | ESLint                                            |
| `npm run format`    | Prettier, write in place                          |
| `npm run check`     | typecheck + lint + format check — run before a PR |

## Architecture

```
src/
  routes/          File-based routes. The filename *is* the URL.
  components/
    landing/       Marketing page sections
    originkit/     Vendored hero + features sections
    dashboard/     Console shell, nav, palette, tables, charts
    dither-kit/    The in-house chart library
    dotcut/        Canvas dithering engine for the landing visuals
    ui/            Shared primitives (button, input, modal, toast, …)
  lib/             Auth, in-memory store, fixtures, small utilities
  index.css        The entire design system — tokens, both polarities
```

### Routing

[TanStack Router](https://tanstack.com/router) in file-based mode. A file in
`src/routes/` becomes a route, and `src/routeTree.gen.ts` is regenerated by the
Vite plugin on every dev run — **never edit it by hand.** Dots are path
separators, so `dashboard.workflows.$workflowId.tsx` serves
`/dashboard/workflows/:workflowId`.

Routes are code-split automatically (`autoCodeSplitting`). The router is
configured in `src/main.tsx` to preload a route's chunk on hover or focus, and
every route inherits a shared error boundary and pending state from
`src/components/route-status.tsx` — so a route that throws degrades to a scoped
panel rather than a blank page.

### State

Three providers wrap the app in `src/routes/__root.tsx`:

- **`AuthProvider`** (`lib/auth.tsx`) — mock session in `localStorage`, with
  simulated latency so pending and error states are real. Route guards read it
  synchronously in `beforeLoad`.
- **`DataProvider`** (`lib/store.tsx`) — the workspace: workflows and
  deployments, seeded from `lib/mock-data.ts`. Writes live in memory and reset
  on reload, by design.
- **`ToastProvider`** (`components/ui/toast.tsx`) — `useToast().toast({ … })`.

Each is shaped so that swapping in a real API touches only the provider, and
none of the consuming components.

### Theming

There is one brand rendered in two polarities, both defined in `src/index.css`:

- `:root` — the light **paper** surface: marketing, auth, 404.
- `.console` — the dark surface, applied by `DashboardShell` (and by the
  product shot embedded in the landing hero).

Every component is written in tokens (`bg-card`, `text-muted-foreground`,
`var(--status-good)`), so **no component knows which polarity it is in**. That
is the rule to preserve: if you find yourself writing a literal hex or a
`dark:` variant in a component, the token is missing — add it to both blocks in
`index.css` instead.

The colour ramp, contrast measurements, and chart tokens are documented inline
at the top of `index.css`.

## Conventions

- **Motion honours `prefers-reduced-motion`, always.** Use `useReducedMotion()`
  from `framer-motion` and pass `initial={reduce ? false : …}`; for CSS
  animations, add a `@media (prefers-reduced-motion: reduce)` rule. This is
  consistent across the codebase and worth keeping that way.
- **Colour is never the only signal.** Status is always colour _plus_ an icon
  _plus_ a text label — see `StateBadge` in `components/dashboard/primitives.tsx`.
- **Comments explain why, not what.** The existing comments carry real design
  reasoning; match that register rather than narrating the code.
- **Reuse the shared primitives.** `PageHeader`, `Panel`, `StatTile`,
  `EmptyState`, `RangeSelector` (`dashboard/primitives.tsx`) and
  `ResourceTable` (`dashboard/resource-table.tsx`) already cover the shape
  almost every console page needs.
- **Adding a console page:** create the route file, then add it to
  `components/dashboard/nav-config.ts`. The sidebar, the breadcrumb, and the
  ⌘K palette are all driven from that one config, so the page becomes
  reachable everywhere at once.

## Keyboard

| Key     | Action                    |
| ------- | ------------------------- |
| `⌘K`    | Command palette           |
| `⌘B`    | Collapse / expand sidebar |
| `↑` `↓` | Move through results      |
| `Esc`   | Close, or clear a filter  |

## Notes

Formatting was adopted after the bulk of the code was written; `npm run format`
was applied across the tree in a single dedicated commit, kept separate so it
never obscures a real change.
